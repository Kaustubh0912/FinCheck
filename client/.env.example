# Copy to client/.env only if the frontend is deployed SEPARATELY from the API
# (e.g. frontend on Vercel, backend on Render). Point it at the backend's /api.
# Leave unset for local dev and single-service deploys — defaults to "/api".
# VITE_API_URL="https://fincheck.onrender.com/api"
